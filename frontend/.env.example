# Куда проксировать /api и /health в dev (если 8000 занят на Windows — используй 8001)
VITE_API_PROXY_TARGET=http://127.0.0.1:8001

# Прямой URL без proxy (production / если proxy не нужен)
# VITE_API_BASE_URL=http://127.0.0.1:8001
